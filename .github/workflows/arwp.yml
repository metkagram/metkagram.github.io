name: ARWP validation

on:
  push:
    branches: [main]
    paths:
      - 'ai/**'
      - 'knowledge/**'
      - 'data/**'
      - 'api/**'
      - 'remote-mcp/**'
      - 'llms.txt'
      - 'scripts/apply-arwp.mjs'
      - 'scripts/stages/render.mjs'
      - '.github/workflows/arwp.yml'
  pull_request:
    paths:
      - 'ai/**'
      - 'knowledge/**'
      - 'data/**'
      - 'api/**'
      - 'remote-mcp/**'
      - 'llms.txt'
      - 'scripts/apply-arwp.mjs'
      - 'scripts/stages/render.mjs'
      - '.github/workflows/arwp.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate Agent-Ready Web Profile
        uses: dkharlanau/agent-ready-web-profile@c1d51698e3765d0817ee14bcbc36c67297457260
        with:
          profile: ai/site-profile.json
