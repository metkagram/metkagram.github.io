# Metkagram dataset publication

Canonical machine-readable dataset metadata: [`dataset.jsonld`](./dataset.jsonld).

Publication lifecycle and DOI state: [`dataset-publication.json`](./dataset-publication.json).

Version 1.0.0 is prepared as the current archival release candidate. Until an external archive issues and resolves a DOI, the canonical state remains `doi-not-issued`.
